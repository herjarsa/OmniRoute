- **Vision bridge**: `auto/best-vision` and `auto/pro-vision` now resolve to the vision category (capability-filtered candidate pool) instead of the flat smart variant, so the describe/reroute target is a model that can actually see images; the vision candidate pool excludes catalog entries whose vision support is overstated (opencode-go/opencode-zen/tokenrouter, forced through the bridge anyway); an `auto/*` reroute target is never blocked by a missing `auto` provider credential row; and claude-wire backends (MiniMax, Z.AI, Kimi) get image URLs resolved to base64 before rerouting (they reject remote URLs, e.g. MiniMax 403 2013).
